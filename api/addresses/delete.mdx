---
title: "Delete an address"
description: "Permanently remove a shipping address."
api: "DELETE https://api.cafeacacias.com/api/addresses/:id"
---

<Warning>
  This action is irreversible. Deleting an address cannot be undone.
</Warning>

## Authentication

<ParamField header="Authorization" type="string" required>
  Bearer token. Format: `Bearer <jwt_token>`
</ParamField>

## Path parameters

<ParamField path="id" type="number" required>
  Numeric ID of the address to delete.
</ParamField>

## Response

Returns `200 OK` with the deleted address object as it existed before deletion.

<ResponseField name="data" type="object">
  The deleted address object.

  <Expandable title="address object">
    <ResponseField name="id" type="number">Numeric ID of the deleted address.</ResponseField>
    <ResponseField name="documentId" type="string">Strapi v5 document ID.</ResponseField>
    <ResponseField name="title" type="string">Address label.</ResponseField>
    <ResponseField name="name" type="string">Recipient name.</ResponseField>
    <ResponseField name="address" type="string">Street address.</ResponseField>
    <ResponseField name="city" type="string">City.</ResponseField>
    <ResponseField name="state" type="string">Region or department.</ResponseField>
    <ResponseField name="postalCode" type="string">Postal code.</ResponseField>
    <ResponseField name="phone" type="string">Phone number.</ResponseField>
    <ResponseField name="dialCode" type="string">Dial code prefix, or `null`.</ResponseField>
    <ResponseField name="createdAt" type="string">ISO 8601 creation timestamp.</ResponseField>
    <ResponseField name="updatedAt" type="string">ISO 8601 last-updated timestamp.</ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="meta" type="object">
  Empty object for single-resource responses.
</ResponseField>

<RequestExample>

```bash cURL
curl --request DELETE \
  --url 'https://api.cafeacacias.com/api/addresses/24' \
  --header 'Authorization: Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...'
```

</RequestExample>

<ResponseExample>

```json 200
{
  "data": {
    "id": 24,
    "documentId": "addr_r4s8t1u5v2",
    "title": "Casa",
    "name": "Sebastián Mora Castillo",
    "address": "Calle 10 # 43-12, Apto 202",
    "city": "Medellín",
    "state": "Antioquia",
    "postalCode": "050022",
    "phone": "3157654321",
    "dialCode": "+57",
    "createdAt": "2026-03-17T10:00:00.000Z",
    "updatedAt": "2026-03-17T11:45:00.000Z"
  },
  "meta": {}
}
```

```json 401
{
  "data": null,
  "error": {
    "status": 401,
    "name": "UnauthorizedError",
    "message": "Missing or invalid credentials",
    "details": {}
  }
}
```

```json 404
{
  "data": null,
  "error": {
    "status": 404,
    "name": "NotFoundError",
    "message": "Not Found",
    "details": {}
  }
}
```

</ResponseExample>
